# ── Gemini Vision API (REQUERIDO para motor primario) ───────────
GEMINI_API_KEY=tu-api-key-aqui

# ── Opcional ────────────────────────────────────────────────────
# GEMINI_MODEL=gemini-2.5-flash        # Modelo a usar (default)
# DEVICE=cpu                            # cpu o cuda (para fallback local)
# PORT=8000                             # Puerto del servidor
