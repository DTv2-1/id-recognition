# Dependencias mínimas para deploy solo con Gemini (sin GPU, sin PyTorch)
# Usar este archivo si NO necesitas fallback a modelos locales.

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pillow>=10.0
numpy>=1.26
google-genai>=1.0

# Tests
pytest>=8.0
httpx>=0.27
